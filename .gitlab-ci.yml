stages:
  - build
  - check

maven-build:
  image: maven:3-jdk-8
  stage: build
  script:
    - mvn compile
  artifacts:
    paths:
      - target

defprev-check:
  image: sudobobo/gitlab-client
  stage: check
  script:
    - ls